{
  "name": "jev-agent-device",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Jev decision loop for mobile QA with agent-device",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "qa": "node src/cli.mjs",
    "check": "node --check src/agent.mjs && node --check src/device.mjs && node --check src/cli.mjs"
  },
  "exports": "./src/index.mjs",
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0",
    "agent-device": "0.21.6"
  }
}
